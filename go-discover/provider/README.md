# go-discover/provider

See: https://github.com/hashicorp/go-discover

## For dev environment startup in VSCode:
Create `.env` file in `.devcontainer` using `.env.example` as an example.

## List

- DNS - simple addr provider that resolves provided hostname.
